# Вариант 42 — Учебные группы «Соберёмся и решим» 🧪 — Матрица прав (операция × роль)

Роли системы: Администратор (admin), Пользователь (user)

Примечание: часть прав для `user` зависит от членства в группе (membership). В таблице ниже `user (member)` — пользователь, являющийся участником конкретной группы; `user (owner)` — владелец группы.

## Таблица прав (кто может что)

- Регистрация/вход: user
- Просмотр списка групп: admin, user
- Создать группу: admin, user
- Редактировать/удалить группу: admin, user(owner)
- Вступить/выйти из группы: user
- Просмотр участников группы: admin, user(member)
- Управлять участниками (кик/роль): admin, user(owner)

- CRUD тем (Topic): admin, user(owner)
- Просмотр тем: admin, user(member)

- CRUD встреч (Meeting): admin, user(owner)
- Просмотр встреч: admin, user(member)

- CRUD материалов (Material): admin, user(owner)
- Просмотр материалов: admin, user(member)

- CRUD задач (Task): admin, user(owner)
- Просмотр задач: admin, user(member)
- Отметить задачу выполненной: admin, user(member)

## Scope-ограничения

- Пользователь видит содержимое только тех групп, где он участник.
- Владелец управляет контентом и участниками только своей группы.
